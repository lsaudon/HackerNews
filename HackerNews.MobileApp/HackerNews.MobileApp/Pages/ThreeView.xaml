<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="HackerNews.MobileApp.Pages.ThreeView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mobileApp="clr-namespace:HackerNews.MobileApp;assembly=HackerNews.MobileApp"
    xmlns:pages="clr-namespace:HackerNews.MobileApp.Pages;assembly=HackerNews.MobileApp"
    Title="{Binding Title}"
    mobileApp:ViewModelLocator.AutoWireViewModel="true"
    x:DataType="pages:ThreeViewModel">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
            <Button Command="{Binding GoToOneViewCommand}" Text="One View" />
            <Button Command="{Binding GoToTwoViewCommand}" Text="Two View" />
            <Button Command="{Binding GoToThreeViewCommand}" Text="Three View" />
            <Button Command="{Binding GoBackViewCommand}" Text="Go back" />
            <Button Command="{Binding GoUntilOneViewModelViewCommand}" Text="Go until one view" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>